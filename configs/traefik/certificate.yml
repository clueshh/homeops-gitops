apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-tls
spec:
  secretName: traefik-tls
  dnsNames:
    - traefik.whitworth.nz
    - '*.lab.whitworth.nz'
  issuerRef:
    name: acme
    kind: Issuer
