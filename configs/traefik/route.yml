---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: traefik-dashboard-route
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: traefik-gateway
      namespace: traefik
  hostnames:
    - traefik.lab.whitworth.nz
  rules:
    # Redirect root -> /dashboard/
    - matches:
        - path:
            type: Exact
            value: /
      filters:
        - type: RequestRedirect
          requestRedirect:
            path:
              type: ReplaceFullPath
              replaceFullPath: /dashboard/
            statusCode: 302

    # Serve dashboard and api from Traefik service
    - matches:
        - path:
            type: PathPrefix
            value: /dashboard
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        # Authentik does currently not support Proxy auth with subpaths
        # https://github.com/goauthentik/authentik/issues/2305
        # - name: authentik-server
        #   namespace: authentik
        #   port: 80
        - name: traefik-internal
          port: 8080
